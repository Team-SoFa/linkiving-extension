import { tv } from 'tailwind-variants';

export const style = tv({
  base: '',
  variants: {
    textSize: {
      sm: 'font-label-sm',
      md: 'font-label-md',
      lg: 'font-label-lg',
      xl: 'font-label-xl',
    },
  },
});
